import React from 'react';
import Accueil from "../components/Accueil"

const accueil = () => {
    return (
        <div className='accueil main' id='accueil'>
            <Accueil/>
        </div>
    );
};

export default accueil;